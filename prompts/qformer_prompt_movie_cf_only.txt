A user's collaborative taste profile is: <UserProfile>. Leverage this profile to predict whether the user would enjoy this movie: <TargetItemID> The semantic profile of this movie is: <Warm_ID> Answer with "Yes" or "No".
A user's collaborative taste profile is: <UserProfile>. Based on this profile, predict whether the user would like this movie: <TargetItemID> The semantic profile of this movie is: <Warm_ID> Answer with "Yes" or "No".
Given a user's collaborative taste profile <UserProfile>, decide whether the user would enjoy this movie: <TargetItemID> The semantic profile of this movie is: <Warm_ID> Answer with "Yes" or "No"
# No item text at all: both <ItemTitleList> and <TargetItemTitle> removed.
#
# Every bit of item and user information now flows through the Q-Former soft
# tokens. This is the cleanest measurement of what the whole CF pipeline
# (MF -> Stage 1 -> Stage 2 -> llm_proj) actually delivers, with no natural-text
# path for LoRA to fall back on.
#
# It is also the harshest setting, so read it as a LOWER BOUND on the channel
# rather than as a target configuration: the frozen LLM has no lexical hook at
# all, and 0.5 here would mean the channel is empty while anything well above
# chance means it carries real signal.
#
# Use together with the other three variants to fill the 2x2:
#     ItemTitleList  TargetItemTitle  file
#          yes            yes         qformer_prompt_movie.txt
#          no             yes         qformer_prompt_movie_no_hist_text.txt
#          yes            no          qformer_prompt_movie_no_target_text.txt
#          no             no          this file
#
# Both <UserProfile> and <TargetItemID> must stay present: encode_rec_features_
# to_llm_v2 only builds merged_embs when BOTH placeholders are in feature_order,
# so dropping either disables soft-token injection entirely.
