model:
  arch: mini_gpt4rec_v2
  model_type: pretrain_vicuna  # legacy key — `_init_llm_model` uses AutoModel, any HF causal LM works.
  # CHANGE C: instruction-tuned backbone. Qwen2-7B-Instruct keeps the
  # instruction-following prior that a Yes/No CTR-reasoning task benefits from.
  # (To revert to the base model, set this back to ".../qwen2-7b-base" and the
  # base-model `prompt_template` below.)
  llm_model: "/content/SigLLM/ckpt/llm/qwen2-7b-instruct"
  prompt_path: "/content/SigLLM/prompts/qformer_prompt_movie_mt.txt"
  # CHANGE C: restore chat formatting using the Qwen2 ChatML template. The
  # prompt body (with <CFTokens>/<ItemTitleList>/<TargetItemTitle> placeholders)
  # is wrapped as the user turn; the model then continues the assistant turn
  # with the "Yes"/"No" label. `add_special_tokens=False` tokenization keeps
  # the <|im_start|>/<|im_end|> control tokens intact as single ids, and
  # `_resolve_soft_token_placeholder` now excludes them so the soft-token slot
  # never collides with chat control tokens.
  #
  # Legacy base-model template (use with qwen2-7b-base):
  #   prompt_template: '{} Answer:'
  prompt_template: "<|im_start|>system\nYou are a helpful movie recommendation assistant. Answer only with \"Yes\" or \"No\".<|im_end|>\n<|im_start|>user\n{}<|im_end|>\n<|im_start|>assistant\n"
  
  freeze_rec: True
  freeze_proj: False

  # Stage 3 ablation switch. When True, target_llm and interacted_llm_flat
  # are zeroed right after llm_proj, so every soft-token slot in the prompt
  # carries a zero vector instead of the projected Q-Former output. Use this
  # to measure the marginal contribution of soft tokens at inference: load a
  # trained checkpoint, flip this to True, re-evaluate. If AUC barely drops,
  # the Q-Former isn't contributing — the LLM is leaning on prompt text alone.
  ablate_soft_tokens: False

  # CoLLM-style 2-step tuning. Each Stage 3 pipeline script overrides this
  # from its own `run.qformer_stage3_step{1,2}` block (see
  # `apply_step{1,2}_overrides` in the two scripts). Leave null at top level —
  # the model only enforces a step policy when this is explicitly 1 or 2.
  tuning_step: null

  lora_config:
    use_lora: True
    # Reverted to r=8 [q,v] after A ablation: r=16 [q,k,v,o] (10M trainable
    # LoRA) overfit on 33k ML1M samples — val uAUC peaked at 0.7048 vs r=8
    # baseline 0.7077, with train loss dropping ~2x faster than r=8.
    # Conclusion: LoRA capacity is NOT the bottleneck; Q-Former Stage 1
    # quality is (now addressed by 8 layers + user-item contrastive).
    r: 8
    alpha: 16
    target_modules: [q_proj, v_proj]
    dropout: 0.05

  max_txt_len: 1024
  end_sym: "###"
  ans_type: "v2"

  proj_token_num: ${model.qformer_config.num_queries}

  # CHANGE A: CoRA-style collaborative injection. Controls how the Q-Former's
  # collaborative queries reach the LLM:
  #   - "soft_token"  : current behaviour — queries projected to <CFTokens> slots.
  #   - "lora_weight" : queries are turned into a per-sample low-rank weight delta
  #                     merged into the LLM's attention projections (parameter
  #                     space). Use a TEXT-ONLY prompt (no <CFTokens>) in this mode.
  #   - "both"        : soft tokens AND weight delta.
  # `cora_alpha` scales the delta (scaling = cora_alpha / num_queries, LoRA-style).
  # `cora_target_modules` defaults to the same projections LoRA adapts.
  # `cora_alpha` sets the ceiling scale (scaling = cora_alpha / num_queries).
  # The injector also normalizes queries to unit L2 and uses a zero-init tanh
  # gate per shape, so the delta starts at exactly 0 and grows only as far as
  # training wants — this is what keeps `both`/`lora_weight` stable. Kept low
  # on purpose; raise cautiously and watch val AUC for divergence.
  cf_injection_mode: "soft_token"
  cora_alpha: 4
  cora_target_modules: ${model.lora_config.target_modules}

  rec_model: "MF"
  user_num: -100
  item_num: -100

  qformer_config:
    use_qformer: True
    qformer_ckpt: "${run.qformer_stage2.output_dir}${run.qformer_stage2.qformer_ckpt_out}" # Q-Former weights after Stage 2 generative pretraining
    llm_proj_ckpt: "${run.qformer_stage2.output_dir}${run.qformer_stage2.proj_ckpt_out}" # Projection weights after Stage 2 generative pretraining
    qformer_d_model: 768
    qformer_output_dim: 768 # Keep Q-Former compact; llm_proj maps this to the LLM hidden size.
    qformer_text_model_name: "bert-base-uncased"
    max_instruction_length: 48
    num_queries: 16
    num_heads: 8
    num_layers: 4
    max_history_length: 10

  rec_config:
    user_num: 839
    item_num: 3256
    embedding_size: 256
    pretrained_path: "/content/SigLLM/ckpt/mf/mf_model.pth"

  # Warm-init checkpoint. Step 2 overrides this with the Step 1 LoRA ckpt;
  # Step 1 overrides it to null. Leave null at top level so a bare config
  # build (e.g. an ablation run) does not silently load a warm-init.
  ckpt: null

datasets:
  movie_ood:
    path: "/content/SigLLM/data/processed/ml-1m/"
    data_type: default
    build_info:
      storage: "/content/SigLLM/data/processed/ml-1m/"

run:
  task: rec_pretrain
  # Pipeline scripts override this from `run.qformer_stage3_step{1,2}`.
  output_dir: "ckpt/qformer_stage3_default"
  device: "cuda"
  seed: 42
  rec_baseline:
    save_file: ${model.rec_config.pretrained_path}
    need_train: True
    warm_or_cold: "warm"
    seed: 2025
    lr: 1e-3
    wd: 1e-4
    embedding_size: ${model.rec_config.embedding_size}
    epoch: 5000
    eval_epoch: 1
    patience: 100
    batch_size: 1024
  qformer_stage1:
    # BLIP-2 representation pretraining: ITC + ITM + ITG using the Q-Former's
    # own text branch (no external BERT). Item-item contrastive (w_ii) is a
    # SigLLM-specific addition for co-watch collaborative signal.
    seed: 2025
    batch_size: 256
    num_workers: 4
    embedding_size: ${model.rec_config.embedding_size}
    user_num: ${model.rec_config.user_num}
    item_num: ${model.rec_config.item_num}
    num_queries: ${model.qformer_config.num_queries}
    num_heads: ${model.qformer_config.num_heads}
    num_layers: ${model.qformer_config.num_layers}
    qformer_d_model: ${model.qformer_config.qformer_d_model}
    qformer_output_dim: ${model.qformer_config.qformer_output_dim}
    qformer_text_model_name: ${model.qformer_config.qformer_text_model_name}
    max_instruction_length: ${model.qformer_config.max_instruction_length}
    item_pair_window: 2
    max_item_item_pairs: null
    # CHANGE F: the user-item contrastive is already wired (include_user_item +
    # w_ui below). The problem was balance: ML1M has ~888K positive user-item
    # pairs vs ~3K item-text pairs, so an uncapped user_item set dominates each
    # flat batch and STARVES the ITC/ITM/ITG item-text alignment (those losses
    # only fire when >=2 item_text samples land in a batch). Cap the user-item
    # pairs to keep batches balanced, and down-weight w_ui so it regularises
    # rather than overwhelms. NOTE: changing max_user_item_pairs requires
    # rebuilding the Q-Former pkls (python -m
    # sigllm.pipelines.multimodal.build_qformer_dataset --cfg-path configs/config.yaml).
    # Sweep w_ui in {0.2, 0.5, 1.0} and the cap in {10000, 20000, 40000}.
    max_user_item_pairs: 20000
    include_user_item: True
    # CHANGE 2d: richer, domain-aware item text (movie wording for ML-1M).
    rich_item_text: True
    item_noun: "movie"
    # CHANGE 2e: more (and harder) ITM negatives (1 = legacy BLIP-2).
    itm_num_neg: 4
    # CHANGE 2f: guarantee >=min_item_text item-text rows per Stage-1 train batch
    # so ITC/ITM/ITG fire with enough in-batch negatives (train loader only).
    balanced_batches: True
    min_item_text_per_batch: 64
    lr: 5e-5
    w_itc: 1.0
    w_itm: 1.0
    w_itg: 1.0
    w_ii: 1.0
    w_ui: 0.5   # CHANGE F: moderate (was 1.0) — regularise without dominating; sweep {0.2,0.5,1.0}
    tau_itc: 0.07
    tau_ii: 0.05
    tau_ui: 0.07
    weight_decay: 1e-3
    debug_batch: True
    debug_batch_max_steps: 10
    early_stopping_patience: 10
    early_stopping_min_delta: 1e-4
    best_checkpoint_name: "qformer_stage1_best_full.pt"
    best_qformer_weights_name: "qformer_stage1_best_qformer.pth"
    log_epoch: 1
    epoch: 100
    pretrained_rec_path: ${model.rec_config.pretrained_path}
    freeze_rec: True
    output_dir: "/content/SigLLM/ckpt/qformer_stage1/"

  qformer_stage2:
    # BLIP-2-style generative pretraining: train Q-Former + linear projection
    # on next-token LM of item-text captions with the LLM fully frozen (no LoRA).
    seed: 2025
    batch_size: 32
    num_workers: 4
    embedding_size: ${model.rec_config.embedding_size}
    user_num: ${model.rec_config.user_num}
    item_num: ${model.rec_config.item_num}
    num_queries: ${model.qformer_config.num_queries}
    num_heads: ${model.qformer_config.num_heads}
    num_layers: ${model.qformer_config.num_layers}
    qformer_d_model: ${model.qformer_config.qformer_d_model}
    qformer_output_dim: ${model.qformer_config.qformer_output_dim}
    qformer_text_model_name: ${model.qformer_config.qformer_text_model_name}
    max_instruction_length: ${model.qformer_config.max_instruction_length}
    llm_model_name: ${model.llm_model}
    pretrained_rec_path: ${model.rec_config.pretrained_path}
    qformer_ckpt_in: "${run.qformer_stage1.output_dir}${run.qformer_stage1.best_qformer_weights_name}"
    qformer_ckpt_out: "qformer_stage2_best_qformer.pth"
    proj_ckpt_out: "qformer_stage2_best_proj.pth"
    # CHANGE 2g: match the Stage-3 instruction-aware forward path ("multimodal");
    # "unimodal" reproduces the legacy BLIP-2 queries-only pretraining.
    qformer_input_mode: "multimodal"
    output_dir: "/content/SigLLM/ckpt/qformer_stage2_qwen2/"
    lr: 1e-4
    weight_decay: 1e-3
    epoch: 50
    log_epoch: 1
    max_caption_length: 64
    early_stopping_patience: 8
    early_stopping_min_delta: 1e-4

  qformer_stage3_step1:
    # CoLLM Step 1 — train LoRA only on text-only prompts (no `<ItemIDList>` /
    # `<TargetItemID>` placeholders). Q-Former + projection are frozen here;
    # they will be unfrozen in Step 2. Only Step 1-specific overrides live
    # here — everything else (optimizer schedule, warmup, batch sizes) is
    # inherited from the top-level `run.*` defaults.
    prompt_path: "/content/SigLLM/prompts/qformer_prompt_movie_text_only.txt"
    output_dir: "/content/SigLLM/ckpt/qformer_stage3_step1_lora_qwen2/"
    best_ckpt_name: "checkpoint_best.pth"
    init_lr: 2e-4
    max_epoch: 5

  qformer_stage3_step2:
    # CoLLM Step 2 — train Q-Former + projection on the full prompt, LoRA
    # loaded frozen from the Step 1 best checkpoint. Symmetric in shape to
    # `qformer_stage3_step1`: only the keys that differ from the top-level
    # defaults are listed here.
    prompt_path: "/content/SigLLM/prompts/qformer_prompt_movie_mt.txt"
    output_dir: "/content/SigLLM/ckpt/qformer_stage3_step2_cie_qwen2/"
    ckpt: "${run.qformer_stage3_step1.output_dir}${run.qformer_stage3_step1.best_ckpt_name}"
    init_lr: 3e-5
    max_epoch: 200

  # Optimization
  lr_sched: "linear_warmup_cosine_lr"
  init_lr: 1e-4
  min_lr: 8e-5
  warmup_lr: 1e-5
  weight_decay: 1e-3
  amp: True
  # CHANGE 2i: extra LR for the Q-Former/projection bridge (lr = init_lr * mult,
  # honored across warmup+cosine). 1.0 = off. Raise (e.g. 3-5) to push more
  # gradient/step into the bridge under the frozen LLM; sweep and watch val uAUC.
  qformer_lr_mult: 1.0
  
  # Training Schedule
  max_epoch: 200
  iters_per_epoch: 400
  batch_size_train: 16
  batch_size_eval: 64
  num_workers: 4
  warmup_steps: 200
  
  # Workflow
  evaluate: False
  mode: "v2"
  train_splits: ["train"]
  valid_splits: ["valid"]
  test_splits: ["test", "test_warm", "test_cold"]
  resume_ckpt_path: null

  # Distributed
  distributed: True
  world_size: 1
  dist_url: "env://"
